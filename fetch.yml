name: Fetch Bhati Attendance Data

on:
  schedule:
    - cron: "*/10 * * * *"   # Every 10 minutes
  workflow_dispatch:           # Also allows manual trigger from GitHub UI

permissions:
  contents: write              # Needed to push data.json back to repo

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install firebase-admin pandas openpyxl

      - name: Run fetch script
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: python fetch_data.py

      - name: Commit and push updated data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json last_fetch.json
          git diff --staged --quiet || git commit -m "Auto-update: $(date '+%d-%m-%Y %H:%M') UTC"
          git push
